name: Re-Fit AI CI

on:
  pull_request:
    # feature -> develop 또는 develop -> main PR 시 실행
    branches: [develop, main]
    types: [opened, synchronize, reopened]
    paths-ignore:
      - '.github/workflows/**'
  push:
    # develop 또는 main으로 push(머지 포함)될 때 실행
    branches: [develop, main]
    paths-ignore:
      - '.github/workflows/**'
  workflow_dispatch:
    # 수동 실행 시 작업을 선택할 수 있는 디스패처 구성
    inputs:
      job_to_run:
        description: 'Which job to run'
        required: true
        type: choice
        options:
          - lint-and-test
          - integration
          - release-dev
          - release-prod
          - all

permissions:
  contents: write  # git-auto-commit-action이 파일을 커밋하고 푸시할 수 있도록 허용

env:
  PYTHON_VERSION: '3.11'
  WORKING_DIRECTORY: 'ai_app'

jobs:

  # 1. PR 검증 단계 (Lint + Test + Build Check)
  lint-and-test:
    if: |
      github.event_name == 'pull_request' ||
      (github.event_name == 'workflow_dispatch' && (github.event.inputs.job_to_run == 'lint-and-test' || github.event.inputs.job_to_run == 'all'))
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      
      # requirements.txt와 pyproject.toml 자동 동기화
      - name: Auto-sync requirements.txt to pyproject.toml
        id: sync-deps
        run: |
          python scripts/sync-to-pyproject.py
          
          # Git diff 확인
          if git diff --quiet pyproject.toml; then
            echo "✅ 동기화 완료 (변경 없음)"
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "🔄 pyproject.toml 업데이트됨!"
            echo "changed=true" >> $GITHUB_OUTPUT
          fi
      
      # .venv 캐시로 uv sync 시간 대폭 단축 (lock 미변경 시 복원만 수행)
      - name: Cache uv virtualenv
        uses: actions/cache@v4
        with:
          path: .venv
          key: uv-venv-${{ runner.os }}-${{ hashFiles('pyproject.toml', 'uv.lock') }}
          restore-keys: |
            uv-venv-${{ runner.os }}-
      
      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock
      
      - name: Install Dependencies
        run: uv sync --locked --extra dev
      
      # 린트 및 자동 수정 적용
      - name: Run Lint and Format (Ruff)
        id: ruff-fix
        run: |
          cd ${{ env.WORKING_DIRECTORY }}
          uv run ruff check . --fix
          uv run ruff format .
          uv run ruff check .
          uv run ruff format --check .
          
          # 변경사항 확인
          cd ..
          if git diff --quiet; then
            echo "✅ 린트 및 포맷팅 통과 (변경 없음)"
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "🔄 린트/포맷팅 자동 수정됨!"
            echo "changed=true" >> $GITHUB_OUTPUT
          fi
      
      # 변경사항(pyproject.toml 동기화 + Ruff 자동 수정)이 있으면 자동 커밋 (PR일 때만)
      - name: Auto-commit changes
        if: (steps.sync-deps.outputs.changed == 'true' || steps.ruff-fix.outputs.changed == 'true') && github.event_name == 'pull_request'
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: Auto-sync dependencies and lint fixes"
          file_pattern: "."
          branch: ${{ github.event.pull_request.head.ref }}
          commit_user_name: "github-actions[bot]"
          commit_user_email: "github-actions[bot]@users.noreply.github.com"
      
      # [Type Check] MyPy를 사용한 정적 타입 분석
      - name: Static Type Check (MyPy)
        run: uv run mypy . --ignore-missing-imports || true
      
      # [Security Scan] Frontend의 audit과 대응되는 보안 검사 추가
      - name: Security Scan (Bandit)
        run: uv run bandit -c pyproject.toml -r ${{ env.WORKING_DIRECTORY }} -x tests/
      
      # [Unit Tests] 단위 테스트 수행
      - name: Run Unit Tests
        run: |
          if [ -d "tests/unit" ]; then
            uv run pytest tests/unit -v --cov=${{ env.WORKING_DIRECTORY }}
          else
            echo "No unit tests found"
          fi

      # [Build Check] 머지 전 빌드 가능 여부 확인 단계 추가
      - name: Build Check
        run: uv run python -m build --wheel
        env:
          NODE_ENV: production # 컨텍스트 유지를 위한 설정
      
      # 기존 Application Start 체크 유지
      - name: Verify Application Starts
        run: |
          export PYTHONPATH=$PYTHONPATH:$(pwd)/${{ env.WORKING_DIRECTORY }}
          timeout 10s uv run python -c "from api.main import app; print('Application loaded successfully')" || true

  # ===========================================
  # 2. 통합 검증 단계 (Integration Tests)
  # ===========================================
  integration:
    if: |
      (github.event_name == 'push' && github.ref == 'refs/heads/develop') ||
      (github.event_name == 'workflow_dispatch' && (github.event.inputs.job_to_run == 'integration' || github.event.inputs.job_to_run == 'all'))
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      
      - name: Cache uv virtualenv
        uses: actions/cache@v4
        with:
          path: .venv
          key: uv-venv-${{ runner.os }}-${{ hashFiles('pyproject.toml', 'uv.lock') }}
          restore-keys: |
            uv-venv-${{ runner.os }}-
      
      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock
      
      # [Integration] develop 브랜치 푸시 시에만 통합 테스트 실행
      - name: Install Dependencies
        run: uv sync --locked --extra dev
      
      - name: Integration Test
        run: |
          if [ -d "tests/integration" ]; then
            uv run pytest tests/integration -v
          else
            echo "⚠️ Integration tests not yet implemented"
          fi

  # ===========================================
  # 3. 개발 환경 릴리즈 (develop 브랜치 - Docker 기반)
  # ===========================================
  release-dev:
    needs: [integration]
    if: |
      always() &&
      needs.integration.result != 'failure' &&
      (
        (github.event_name == 'push' && github.ref == 'refs/heads/develop') ||
        (github.event_name == 'workflow_dispatch' && (github.event.inputs.job_to_run == 'release-dev' || github.event.inputs.job_to_run == 'all'))
      )
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.meta.outputs.image_tag }}
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-northeast-2

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Generate Image Metadata
        id: meta
        run: |
          SHA_SHORT="${GITHUB_SHA::7}"
          IMAGE_TAG="develop-${SHA_SHORT}"
          FULL_IMAGE="${{ steps.login-ecr.outputs.registry }}/${{ secrets.ECR_REPOSITORY_AI }}:${IMAGE_TAG}"
          LATEST_IMAGE="${{ steps.login-ecr.outputs.registry }}/${{ secrets.ECR_REPOSITORY_AI }}:develop-latest"

          echo "image_tag=${IMAGE_TAG}" >> $GITHUB_OUTPUT
          echo "full_image=${FULL_IMAGE}" >> $GITHUB_OUTPUT
          echo "latest_image=${LATEST_IMAGE}" >> $GITHUB_OUTPUT
          echo "sha_short=${SHA_SHORT}" >> $GITHUB_OUTPUT
          echo "🐳 Docker Image: ${FULL_IMAGE}"

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ steps.meta.outputs.full_image }}
            ${{ steps.meta.outputs.latest_image }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/arm64

      - name: Trigger CD-Dev Workflow
        if: success()
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.PAT }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'cd-dev.yml',
              ref: 'develop',
              inputs: {
                image_tag: '${{ steps.meta.outputs.image_tag }}',
                sha: '${{ github.sha }}',
                run_id: '${{ github.run_id }}'
              }
            });
            console.log('✅ CD-Dev workflow triggered successfully');

  # ===========================================
  # 4. 프로덕션 릴리즈 (main push 시 자동 - Docker + cd-prod)
  # ===========================================
  release-prod:
    if: |
      (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
      (github.event_name == 'workflow_dispatch' && (github.event.inputs.job_to_run == 'release-prod' || github.event.inputs.job_to_run == 'all'))
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      
      - name: Cache uv virtualenv
        uses: actions/cache@v4
        with:
          path: .venv
          key: uv-venv-${{ runner.os }}-${{ hashFiles('pyproject.toml', 'uv.lock') }}
          restore-keys: |
            uv-venv-${{ runner.os }}-
      
      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true
          cache-dependency-glob: |
            pyproject.toml
            uv.lock
      
      - name: Install dependencies
        run: uv sync --locked --extra dev
      
      - name: Run Full Tests
        run: |
          if [ -d "tests" ] || [ -d "${{ env.WORKING_DIRECTORY }}/tests" ]; then
            if [ -d "tests" ]; then
              uv run pytest tests/ -v || echo "Tests failed but continuing"
            else
              uv run pytest ${{ env.WORKING_DIRECTORY }}/tests -v || echo "Tests failed but continuing"
            fi
          else
            echo "No tests found, skipping"
          fi
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-northeast-2
      
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Docker image tag
        id: docker-meta
        run: |
          SHA_SHORT="${GITHUB_SHA::7}"
          IMAGE_TAG="main-${SHA_SHORT}"
          echo "image_tag=${IMAGE_TAG}" >> $GITHUB_OUTPUT
          echo "sha_short=${SHA_SHORT}" >> $GITHUB_OUTPUT
          echo "🐳 Docker image tag: ${IMAGE_TAG}"
      
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ steps.login-ecr.outputs.registry }}/${{ secrets.ECR_REPOSITORY_AI }}:${{ steps.docker-meta.outputs.image_tag }}
            ${{ steps.login-ecr.outputs.registry }}/${{ secrets.ECR_REPOSITORY_AI }}:main-latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
          platforms: linux/arm64
      
      - name: Trigger CD-Prod Workflow
        if: success()
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.PAT }}
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'cd-prod.yml',
              ref: 'main',
              inputs: {
                use_existing_image: 'true',
                image_tag: '${{ steps.docker-meta.outputs.image_tag }}'
              }
            });
            console.log('✅ CD-Prod workflow triggered (use_existing_image=true)');